# 更新日志

本项目版本由用户显式指定；未升版前的改动记录在 `Unreleased` 分段。

## 2.6.0 (2026-09-25)

### 新增
- **停止时隐藏状态栏**：设置页新增开关（默认关），开启后状态栏仅在连点运行中显示、停止自动隐藏

### 修复
- 状态栏点击/拖动不再抢焦点（`focusable: false` + `ready-to-show` 后重设置顶，兼顾不抢焦点与置顶）
- 消除状态栏显隐切换时的闪烁：改为窗口常驻 + 胶囊显隐 + 鼠标穿透，规避透明窗口 `show` 闪帧与 `setOpacity` 冲突

## 2.5.0 (2026-09-24)

### 新增
- **置顶状态栏**：屏幕置顶胶囊条实时显示「运行中 / 已停止」（iOS 风格，随主题换色），可覆盖无边框全屏/最大化窗口；支持拖动移动位置（拖动期间临时暂停连点、松手自动恢复），位置记忆于 config.json；设置页新增「置顶状态栏」开关（默认开启）

## 2.4.0 (2026-09-06)

### 新增
- **托盘状态图标**：系统托盘新增应用图标，随连点状态切换——闲置＝浅灰线条光标、运行＝绿色线条光标（iOS/macOS 菜单栏风格）；悬停提示同步状态与停止热键；右键菜单含"显示主界面 / 退出"，单击托盘可聚焦主窗口
- **启停提示音**：启动连点时升调双音、停止时单音低音提示，帮助在全屏/离屏场景察觉运行状态
- **设置栏**：界面新增"设置"卡片，可开关"状态提示音"（默认开启），配置持久化于 config.json（`soundOn`）
- 窗口 ready-to-show 后再显示，消除启动白屏

### 修复
- 关闭应用（退出）时不再误响停止提示音——仅在确实由"运行态"转停止时播报

## 2.3.0

### 重构
- 渲染层移除 naive-ui，全部控件手写（切换按钮 / 数字步进器 / 通用按钮），体积与首屏性能改善
- 界面全面 iOS 化：systemBlue 主色 + 分组卡片材质、Inter + MiSans 字体、深浅双主题

### 新增
- 管理员权限自动提权与检测（解决 UIPI 导致部分应用点击无效）
- 深色/浅色主题切换；原生标题栏与窗口启动背景随主题
- 窗口 ready-to-show 后再显示，消除启动白屏

## 更早版本（摘要）

- 2.x：F 键键盘连点、同一按键切换启停、单实例锁、点击间隔时间戳补偿、点击位置拾取、全局快捷键自定义
- 1.x / 0.x：Electron + koffi(SendInput) 连点初版、图标与基础 UI 迭代
